# --- VCS / editors ---
.git
.github
.gitmodules
**/.DS_Store
**/.idea
**/.vscode

# --- dependencies (reinstall inside the image) ---
**/node_modules

# --- build outputs / caches ---
**/dist
**/build
**/.cache
coverage
tmp
*.log

# --- artifacts you don’t want in context ---
release-packages

# keep the helm chart
!ci/helm-chart/**
# keep code-server build + dev scripts (needed for npm run build)
!ci/build
!ci/build/**
!ci/dev
!ci/dev/**